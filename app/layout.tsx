import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/sonner";
import { TooltipProvider } from "@/components/ui/tooltip";

const geistSans = Geist({
  variable: "--font-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "TabAndRate — AI Review Funnel",
    template: "%s | TabAndRate",
  },
  description:
    "Help your customers write the perfect Google review in seconds with AI. Generate more 5-star reviews effortlessly.",
  keywords: ["google reviews", "ai reviews", "review funnel", "local business", "qr code"],
  openGraph: {
    title: "TabAndRate — AI Review Funnel",
    description: "Generate more 5-star Google reviews effortlessly with AI.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased font-sans`}
      >
        <TooltipProvider>
          {children}
          <Toaster richColors position="top-right" />
        </TooltipProvider>
      </body>
    </html>
  );
}
